{
  "name": "nft-games",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow npm",
    "dev": "cd packages/nextjs && npm run dev",
    "build": "cd packages/nextjs && npm run build",
    "chain": "cd packages/foundry && forge script ...",
    "deploy": "cd packages/foundry && forge script ..."
  },
  "engines": {
    "node": ">=20.18.3"
  }
}
